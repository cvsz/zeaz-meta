resources:
  - ../../base
namePrefix: prod-
commonLabels:
  app.kubernetes.io/part-of: zeaz-platform
  zeaz.dev/environment: prod
patches:
  - target:
      kind: Deployment
      name: zeaz-service
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 5
