server {
  bind_address = "0.0.0.0"
  bind_port = "8081"
  trust_domain = "zeaz.dev"
  data_dir = "/run/spire/data"
  log_level = "INFO"
}
plugins { DataStore "sql" { plugin_data { database_type = "postgres" connection_string = "postgres://spire:spire@postgres:5432/spire?sslmode=require" } } }
